<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página de Atualização - Nova Agro</title>
    <link rel="icon" href="/img/logo.png" type="image/png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #8ef68a;
        }
        .update-container {
            background-color: white;
            border: 1px solid #ddd;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .btn-primary {
            background-color: #28a745 !important;
            border-color: #28a745 !important;
        }
        .btn-primary:hover {
            background-color: #218838 !important;
            border-color: #1e7e34 !important;
        }
        .form-control {
            margin-bottom: 15px;
        }
    </style>
</head>
<body>

    <div class="update-container">
        <h2 class="text-center text-green mb-4">Atualizar Conteúdo</h2>
        <form action="/atualizarNoticias" method="POST">
            <div class="mb-3">
                <input type="text" class="form-control" id="codigoSecreto" name="codigoSecreto" placeholder="Digite o código de administrador" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Atualizar</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
